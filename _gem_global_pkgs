# Note:
# nokogiriが必要なgemがある場合、rubyのバージョンによっては
#   gem install nokogiri -v VER
# でVER指定が必要なケースがあるかも
# （他にもrubyのverが古いと同様に -v 指定が必要なgemはありそう）
export MY_GEM_GLOBAL_PKGS=( \
  "statelint" \
  "rubocop" \
  "solargraph" \
  "reek" \
  "rufo" \
)
