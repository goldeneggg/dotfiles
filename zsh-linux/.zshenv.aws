# use $HOME/.aws/config file for awscli (ex: region, etc)
# use $HOME/.aws/credentials file for awscli (ex: aws_access_key_id, etc)
export AWS_SDK_LOAD_CONFIG=1
#export AWS_DEFAULT_REGION=ap-northeast-1
