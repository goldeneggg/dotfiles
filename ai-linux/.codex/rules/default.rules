# >>>>>>>>>> common global rules
prefix_rule(
    pattern = [["sudo", "\\sudo"]],
    decision = "forbidden",
    justification = "Synced from Claude permissions.deny: Bash(sudo *)",
)

prefix_rule(
    pattern = [["chmod", "\\chmod"]],
    decision = "prompt",
    justification = "Synced from Claude permissions.ask: Bash(chmod *)",
)

prefix_rule(
    pattern = [["chown", "\\chown"]],
    decision = "prompt",
    justification = "Synced from Claude permissions.ask: Bash(chown *)",
)

prefix_rule(
    pattern = [["cp", "\\cp"]],
    decision = "prompt",
    justification = "Synced from Claude permissions.ask: Bash(cp *)",
)

prefix_rule(
    pattern = [["curl", "\\curl"]],
    decision = "prompt",
    justification = "Synced from Claude permissions.ask: Bash(curl *)",
)

prefix_rule(
    pattern = [["ps", "\\ps"]],
    decision = "allow",
    justification = "Synced from Claude permissions.ask: Bash(ps *)",
)

prefix_rule(
    pattern = [["pgrep", "\\pgrep"]],
    decision = "allow",
    justification = "Synced from Claude permissions.ask: Bash(pgrep *)",
)

prefix_rule(
    pattern = [["kill", "\\kill"]],
    decision = "prompt",
    justification = "Synced from Claude permissions.ask: Bash(kill *)",
)

prefix_rule(
    pattern = [["mv", "\\mv"]],
    decision = "prompt",
    justification = "Synced from Claude permissions.ask: Bash(mv *)",
)

prefix_rule(
    pattern = [["rm", "\\rm"]],
    decision = "prompt",
    justification = "Synced from Claude permissions.ask: Bash(rm *)",
)

prefix_rule(
    pattern = [["rmdir", "\\rmdir"]],
    decision = "prompt",
    justification = "Synced from Claude permissions.ask: Bash(rmdir *)",
)

prefix_rule(
    pattern = [["touch", "\\touch"]],
    decision = "allow",
    justification = "Synced from Claude permissions.ask: Bash(touch *)",
)

prefix_rule(
    pattern = [["wget", "\\wget"]],
    decision = "prompt",
    justification = "Synced from Claude permissions.ask: Bash(wget *)",
)

prefix_rule(
    pattern = [["git", "\\git"], "push"],
    decision = "prompt",
    justification = "Synced from Claude permissions.ask: Bash(git push *)",
)

prefix_rule(
    pattern = [["fd", "\\fd"]],
    decision = "allow",
    justification = "Synced from Claude permissions.allow: Bash(fd *)",
)

prefix_rule(
    pattern = [["rg", "\\rg"]],
    decision = "allow",
    justification = "Synced from Claude permissions.allow: Bash(rg *)",
)

prefix_rule(
    pattern = [["rga", "\\rga"]],
    decision = "allow",
    justification = "Synced from Claude permissions.allow: Bash(rga *)",
)

prefix_rule(
    pattern = [["cat", "\\cat"]],
    decision = "allow",
    justification = "Synced from Claude permissions.allow: Bash(cat *)",
)

prefix_rule(
    pattern = [["head", "\\head"]],
    decision = "allow",
    justification = "Synced from Claude permissions.allow: Bash(head *)",
)

prefix_rule(
    pattern = [["tail", "\\tail"]],
    decision = "allow",
    justification = "Synced from Claude permissions.allow: Bash(tail *)",
)

prefix_rule(
    pattern = [["less", "\\less"]],
    decision = "allow",
    justification = "Synced from Claude permissions.allow: Bash(less *)",
)

prefix_rule(
    pattern = [["sort", "\\sort"]],
    decision = "allow",
    justification = "Synced from Claude permissions.allow: Bash(sort *)",
)

prefix_rule(
    pattern = [["uniq", "\\uniq"]],
    decision = "allow",
    justification = "Synced from Claude permissions.allow: Bash(uniq *)",
)

prefix_rule(
    pattern = [["awk", "\\awk"]],
    decision = "allow",
    justification = "Synced from Claude permissions.allow: Bash(awk *)",
)

prefix_rule(
    pattern = [["sed", "\\sed"]],
    decision = "allow",
    justification = "Synced from Claude permissions.allow: Bash(sed *)",
)

prefix_rule(
    pattern = [["find", "\\find"]],
    decision = "allow",
    justification = "Synced from Claude permissions.allow: Bash(find *)",
)

prefix_rule(
    pattern = [["grep", "\\grep"]],
    decision = "allow",
    justification = "Synced from Claude permissions.allow: Bash(grep *)",
)

prefix_rule(
    pattern = [["pwd", "\\pwd"]],
    decision = "allow",
    justification = "Synced from Claude permissions.allow: Bash(pwd *)",
)

prefix_rule(
    pattern = [["ls", "\\ls"]],
    decision = "allow",
    justification = "Synced from Claude permissions.allow: Bash(ls *)",
)

prefix_rule(
    pattern = [["tree", "\\tree"]],
    decision = "allow",
    justification = "Synced from Claude permissions.allow: Bash(tree *)",
)

prefix_rule(
    pattern = [["wc", "\\wc"]],
    decision = "allow",
    justification = "Synced from Claude permissions.allow: Bash(wc *)",
)

prefix_rule(
    pattern = [["stat", "\\stat"]],
    decision = "allow",
    justification = "Synced from Claude permissions.allow: Bash(stat *)",
)

prefix_rule(
    pattern = [["file", "\\file"]],
    decision = "allow",
    justification = "Synced from Claude permissions.allow: Bash(file *)",
)

prefix_rule(
    pattern = [["printf", "\\printf"]],
    decision = "allow",
    justification = "Synced from Claude permissions.allow: Bash(printf:*)",
)

prefix_rule(
    pattern = [["history", "\\history"]],
    decision = "allow",
    justification = "Synced from Claude permissions.allow: Bash(history:*)",
)

prefix_rule(
    pattern = [["diff", "\\diff"]],
    decision = "allow",
    justification = "Synced from Claude permissions.allow: Bash(diff:*)",
)

prefix_rule(
    pattern = [["cd", "\\cd"]],
    decision = "allow",
    justification = "Synced from Claude permissions.allow: Bash(cd *)",
)

prefix_rule(
    pattern = [["date", "\\date"]],
    decision = "allow",
    justification = "Synced from Claude permissions.allow: Bash(date *)",
)

prefix_rule(
    pattern = [["whoami", "\\whoami"]],
    decision = "allow",
    justification = "Synced from Claude permissions.allow: Bash(whoami *)",
)

prefix_rule(
    pattern = [["hostname", "\\hostname"]],
    decision = "allow",
    justification = "Synced from Claude permissions.allow: Bash(hostname *)",
)

prefix_rule(
    pattern = [["uname", "\\uname"]],
    decision = "allow",
    justification = "Synced from Claude permissions.allow: Bash(uname *)",
)

prefix_rule(
    pattern = [["id", "\\id"]],
    decision = "allow",
    justification = "Synced from Claude permissions.allow: Bash(id *)",
)

prefix_rule(
    pattern = [["bash", "\\bash"], "-n"],
    decision = "allow",
    justification = "Synced from Claude permissions.allow: Bash(bash -n *)",
)

prefix_rule(
    pattern = [["gh", "\\gh"], "issue", "list"],
    decision = "allow",
    justification = "Synced from Claude permissions.allow: Bash(gh issue list *)",
)

prefix_rule(
    pattern = [["gh", "\\gh"], "issue", "view"],
    decision = "allow",
    justification = "Synced from Claude permissions.allow: Bash(gh issue view *)",
)

prefix_rule(
    pattern = [["gh", "\\gh"], "issue", "status"],
    decision = "allow",
    justification = "Synced from Claude permissions.allow: Bash(gh issue status *)",
)

prefix_rule(
    pattern = [["gh", "\\gh"], "pr", "list"],
    decision = "allow",
    justification = "Synced from Claude permissions.allow: Bash(gh pr list *)",
)

prefix_rule(
    pattern = [["gh", "\\gh"], "pr", "view"],
    decision = "allow",
    justification = "Synced from Claude permissions.allow: Bash(gh pr view *)",
)

prefix_rule(
    pattern = [["gh", "\\gh"], "pr", "status"],
    decision = "allow",
    justification = "Synced from Claude permissions.allow: Bash(gh pr status *)",
)

prefix_rule(
    pattern = [["gh", "\\gh"], "pr", "diff"],
    decision = "allow",
    justification = "Synced from Claude permissions.allow: Bash(gh pr diff *)",
)

prefix_rule(
    pattern = [["gh", "\\gh"], "run", "list"],
    decision = "allow",
    justification = "Synced from Claude permissions.allow: Bash(gh run list *)",
)

prefix_rule(
    pattern = [["gh", "\\gh"], "run", "view"],
    decision = "allow",
    justification = "Synced from Claude permissions.allow: Bash(gh run view *)",
)

prefix_rule(
    pattern = [["gh", "\\gh"], "workflow", "list"],
    decision = "allow",
    justification = "Synced from Claude permissions.allow: Bash(gh workflow list *)",
)

prefix_rule(
    pattern = [["gh", "\\gh"], "workflow", "view"],
    decision = "allow",
    justification = "Synced from Claude permissions.allow: Bash(gh workflow view *)",
)

prefix_rule(
    pattern = [["gh", "\\gh"], "search"],
    decision = "allow",
    justification = "Synced from Claude permissions.allow: Bash(gh search *)",
)

prefix_rule(
    pattern = [["git", "\\git"], "branch"],
    decision = "allow",
    justification = "Synced from Claude permissions.allow: Bash(git branch *)",
)

prefix_rule(
    pattern = [["git", "\\git"], "diff"],
    decision = "allow",
    justification = "Synced from Claude permissions.allow: Bash(git diff *)",
)

prefix_rule(
    pattern = [["git", "\\git"], "show"],
    decision = "allow",
    justification = "Synced from Claude permissions.allow: Bash(git show *)",
)

prefix_rule(
    pattern = [["git", "\\git"], "log"],
    decision = "allow",
    justification = "Synced from Claude permissions.allow: Bash(git log *)",
)

prefix_rule(
    pattern = [["git", "\\git"], "status"],
    decision = "allow",
    justification = "Synced from Claude permissions.allow: Bash(git status *)",
)

prefix_rule(
    pattern = [["git", "\\git"], "fetch"],
    decision = "allow",
    justification = "Synced from Claude permissions.allow: Bash(git fetch *)",
)

prefix_rule(
    pattern = [["git", "\\git"], "add"],
    decision = "allow",
    justification = "Synced from Claude permissions.allow: Bash(git add *)",
)

prefix_rule(
    pattern = [["git", "\\git"], "commit"],
    decision = "allow",
    justification = "Synced from Claude permissions.allow: Bash(git commit *)",
)
# <<<<<<<<<< common global rules
